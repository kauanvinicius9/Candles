spring.application.name=candleshop

spring.datasource.url=jdbc:postgresql://localhost:5432/reviva
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=8080

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:saleskauan308@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

loja.email.destino=saleskauan308@gmail.com

mercadopago.access-token=${MP_ACCESS_TOKEN:TEST-0000000000000000-000000-00000000000000000000000000000000-000000000}
# Trocar para token real quando subir pra produção
mercadopago.webhook-secret=${MP_WEBHOOK_SECRET:}

cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:4200}
