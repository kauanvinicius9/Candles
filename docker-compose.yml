version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: app-frontend
   
    networks:
      - app-network

networks:
  app-network:
    driver: bridge